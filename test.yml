---
- name: Get REST o/p
  connection: local
  gather_facts: false
  hosts: localhost
  tasks:
    - name: print a sample message
      debug:
        msg: "Test playbook"

    - name: ask for message
      uri:
        url: http://someurl
        return_content: yes
      register: restoutput
